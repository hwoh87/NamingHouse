<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      API 30+ 패키지 가시성 — 형제 앱(삼라만상 만세력) 설치 여부를 보고
      배너를 '열기'/'무료'로 가르기 위해 이 패키지만 조회할 수 있게 연다.
    -->
    <queries>
        <package android:name="com.samramanshang.manseryeok" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NamingHouse">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          AdMob 앱 ID. 광고 단위 ID 와 달리 이건 빌드 타입을 가리지 않는다 —
          디버그/릴리즈 구분은 AdIds 의 단위 ID 쪽에서 한다.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1799447209671601~1608111589" />

        <!-- 감명서 이미지 공유 — 캐시의 shared/ 만 내보낸다 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
