<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose 가 그리기 전(스플래시)과 시스템 바 뒤에 보이는 창 배경.
      values-night 에서 어두운 색으로 덮어써야 다크 모드에서 흰 화면이 번쩍이지 않는다.
    -->
    <style name="Theme.NamingHouse" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
